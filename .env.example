# Azure OpenAI (required — global default, used when a project has no per-project endpoint)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_API_KEY=your-azure-api-key

# llimit admin (required)
LLIMIT_ADMIN_TOKEN=your-admin-secret-token

# Database (optional, defaults to llimit.db in working directory)
# LLIMIT_DB_PATH=/data/llimit.db

# ── OAuth / Azure AD user login (optional) ──
# When set, users can sign in via Microsoft and create personal API keys.
# Leave unset to disable the user portal entirely.

# Azure AD (Entra) application registration
# AZURE_AD_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# AZURE_AD_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# AZURE_AD_CLIENT_SECRET=your-client-secret

# Public base URL of this service — must match the redirect URI registered in Azure AD.
# The OAuth callback will be registered as: {LLIMIT_BASE_URL}/auth/callback
# LLIMIT_BASE_URL=https://llimit.example.com

# Corporate email domain (optional).
# Users whose email ends with @<AZURE_AD_DOMAIN> get a short username (the part before @).
# All other users are identified by their full email address.
# AZURE_AD_DOMAIN=example.com
